# Task
Create a python script that, when executed, prints exactly:

This Harness is Working

Requirements:
- The script must run with `python <path>` and print that exact line.
- Write the file via the write_file tool to admin/orchestrator/temp/harness_test.py
- It must be runnable and contain working code.
- No comments needed; just working code.
