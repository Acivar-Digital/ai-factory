[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] remember({"note": "Plan for refactoring check_plan_invariants in factory/temp/factory/infra/validation.py:\n1. Extract workplan resolution logic into _resolve_workplan_groups(plan) helper\n2. Extract task file invariant checking into _check_task_file_invariants(tasks, seen) helper\n3. Simplify check_plan_invariants to call these two helpers (CC will drop from 13 to ≤5)\n4. Preserve docstring, comments, and all existing behavior\n5. Verify with verify_edit after changes"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants", "new_function_code": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    groups = _resolve_workplan_groups(plan)\n    if not groups:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n        return violations\n    tasks = _collect_tasks(groups)\n    _check_task_file_invariants(tasks, violations)\n    return violations\n\n\ndef _resolve_workplan_groups(plan) -> list:\n    \"\"\"Return the groups list from plan.workplan or plan.strategy.parallelisable_workplan.\"\"\"\n    workplan = getattr(plan, 'workplan', None)\n    if not workplan:\n        strategy = getattr(plan, 'strategy', None)\n        if strategy:\n            workplan = getattr(strategy, 'parallelisable_workplan', None)\n    return getattr(workplan, 'groups', []) if workplan else []\n\n\ndef _collect_tasks(groups) -> list:\n    \"\"\"Flatten tasks from all groups.\"\"\"\n    tasks = []\n    for group in groups or []:\n        tasks.extend(getattr(group, 'tasks', []) or [])\n    return tasks\n\n\ndef _check_task_file_invariants(tasks, violations):\n    \"\"\"Check that each task has exactly 1 file and no file collisions.\"\"\"\n    seen: set[str] = set()\n    for task in tasks:\n        fps = getattr(task, 'file_paths', None) or []\n        if len(fps) != 1:\n            violations.append(f\"task {getattr(task, 'id', '?')} lists {len(fps)} files (exactly 1 required)\")\n        for fp in fps:\n            if fp in seen:\n                violations.append(f'file collision: {fp} in multiple tasks')\n            seen.add(fp)"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants"})

[tool-call] final_result({"status": "done", "task_id": "a101k", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored check_plan_invariants from CC=13 to CC=2 by extracting three helpers: _resolve_workplan_groups, _collect_tasks, _check_task_file_invariants. Guard clauses and early returns used to flatten control flow.", "notes": "Refactoring complete. check_plan_invariants CC reduced from 13 to 2 (well under ≤10 target). Three private helpers extracted: _resolve_workplan_groups (workplan resolution), _collect_tasks (task flattening), _check_task_file_invariants (file count + collision checks). All original behavior preserved.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true})