[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] remember({"note": "Plan: Refactor `_downstream_closure` in factory/temp/factory/infra/validation.py to reduce CC to ≤5. Decompose into 3 private helpers: _build_task_group_map (CC≤5), _seed_stack (CC=2), _traverse_dependents (CC≤5). Main function uses guard clauses (CC≤3). Preserve docstring and signature. Add helpers between _downstream_closure and red_team_passed."})

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_downstream_closure", "new_function_code": "def _downstream_closure(failing: set[str], groups: list[WorkGroup]) -> set[str]:\n    \"\"\"Forward-reachable task set from `failing`: each failing task plus every\n    task in any downstream (dependent) WorkGroup. Bounds re-execution so a bad\n    upstream task re-runs its dependents, but untouched work is preserved.\"\"\"\n    if not failing or not groups:\n        return set(failing)\n    task_group, by_id, dependents = _build_task_group_map(groups)\n    stack = _seed_stack(failing, task_group)\n    return _traverse_dependents(stack, by_id, dependents)\n\n\ndef _build_task_group_map(groups: list[WorkGroup]) -> tuple[dict[str, str], dict[str, WorkGroup], dict[str, list[str]]]:\n    \"\"\"Build lookup maps for task-to-group, group-by-id, and dependents adjacency.\"\"\"\n    task_group: dict[str, str] = {}\n    by_id = {g.id: g for g in groups}\n    dependents: dict[str, list[str]] = {g.id: [] for g in groups}\n    for g in groups:\n        for t in g.tasks:\n            task_group[t.id] = g.id\n        for d in g.depends_on:\n            if d in dependents:\n                dependents[d].append(g.id)\n    return task_group, by_id, dependents\n\n\ndef _seed_stack(failing: set[str], task_group: dict[str, str]) -> list[str]:\n    \"\"\"Return group IDs for all failing tasks that have a known group.\"\"\"\n    return [task_group[t] for t in failing if t in task_group]\n\n\ndef _traverse_dependents(stack: list[str], by_id: dict[str, WorkGroup], dependents: dict[str, list[str]]) -> set[str]:\n    \"\"\"Walk the dependency graph from the stack, collecting all reachable task IDs.\"\"\"\n    out: set[str] = set(stack)\n    seen: set[str] = set()\n    while stack:\n        gid = stack.pop()\n        if gid in seen:\n            continue\n        seen.add(gid)\n        for dep in dependents[gid]:\n            for t in by_id[dep].tasks:\n                out.add(t.id)\n            stack.append(dep)\n    return out"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_downstream_closure"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_build_task_group_map"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_seed_stack"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_traverse_dependents"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 1, "end_line": 80})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 80, "end_line": 120})

[tool-call] final_result({"status": "done", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored _downstream_closure from CC=13 to CC=3 by extracting 3 private helpers: _build_task_group_map (CC=5), _seed_stack (CC=2), _traverse_dependents (CC=5). Main function uses guard clause and delegates to helpers. All AST checks pass.", "notes": "Refactoring complete. _downstream_closure CC=3, all helpers CC≤5. Guard clause handles empty inputs. Signature and docstring preserved. Helpers inserted between _downstream_closure and red_team_passed.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true, "verdict_errors": "", "verdict_diff": "", "task_id": "reduce-cc-downstream_closure", "dep_pointers": []})