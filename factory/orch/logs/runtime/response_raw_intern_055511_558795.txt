[tool-call] remember({"note": "Task: Refactor `_extract_trigger_labels` in forecast_store.py from CC=7 to ≤5. Strategy: Extract two private helpers: `_get_events_from_scored` (handles type dispatch for scored input) and `_get_triggers_from_event` (handles type dispatch for each event). This reduces `_extract_trigger_labels` to a simple loop with CC=2. All helpers stay ≤5 CC. No new imports needed. Only modify `_extract_trigger_labels` and add two new helpers in the same file."})

[tool-call] read_file({"relative_path": "factory/temp/src2/interfaces/telegram/chronomancer/forecast_store.py"})

[text] Now I'll apply the refactoring. I'll extract two private helpers to reduce `_extract_trigger_labels` to CC≤5.

[tool-call] replace_function({"relative_path": "factory/temp/src2/interfaces/telegram/chronomancer/forecast_store.py", "function_name": "_extract_trigger_labels", "new_function_code": "def _get_events_from_scored(scored: Any) -> list[Any]:\n    if isinstance(scored, dict):\n        return scored.get(\"events\", [])\n    return getattr(scored, \"events\", [])\n\n\ndef _get_triggers_from_event(event: Any) -> list[str]:\n    if isinstance(event, dict):\n        return event.get(\"triggers\", [])\n    if hasattr(event, \"triggers\"):\n        return list(event.triggers)\n    return []\n\n\ndef _extract_trigger_labels(scored: dict) -> list[str]:\n    triggers = set()\n    for event in _get_events_from_scored(scored):\n        for t in _get_triggers_from_event(event):\n            triggers.add(t)\n    return sorted(triggers)"})

[tool-call] verify_edit({"relative_path": "factory/temp/src2/interfaces/telegram/chronomancer/forecast_store.py", "function_name": "_extract_trigger_labels"})

[tool-call] final_result({"task_id": "refactor-extract-trigger-labels", "status": "done", "files_changed": ["factory/temp/src2/interfaces/telegram/chronomancer/forecast_store.py"], "diff_summary": "Refactored `_extract_trigger_labels` from CC=7 to CC=3 by extracting two private helpers: `_get_events_from_scored` (type dispatch for scored input) and `_get_triggers_from_event` (type dispatch for each event). The main function now has a single flat loop with CC=3.", "notes": "Extracted two helpers to reduce CC: `_get_events_from_scored` handles dict vs object type dispatch for the scored input, and `_get_triggers_from_event` handles dict vs object type dispatch for each event. The main `_extract_trigger_labels` function is now a simple loop with CC=3 (well within the ≤5 target). No new imports added. No dict dispatch or hallucinated helpers used.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true})