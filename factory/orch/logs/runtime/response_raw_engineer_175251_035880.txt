[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] remember({"note": "Plan: Refactor check_plan_invariants in factory/temp/factory/infra/validation.py to CC≤5. Extract workplan resolution into _resolve_workplan(), task collection into _collect_tasks(), and file invariant checks into _check_task_file_invariants(). Use replace_function for the main function and add new helper functions. The current file still has the original CC=13 version."})

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants", "new_function_code": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    workplan = _resolve_workplan(plan)\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n        return violations\n    tasks = _collect_tasks(workplan)\n    _check_task_file_invariants(tasks, violations)\n    return violations"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 1, "end_line": 50})

[tool-call] replace_text({"relative_path": "factory/temp/factory/infra/validation.py", "target_text": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    workplan = _resolve_workplan(plan)\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n        return violations\n    tasks = _collect_tasks(workplan)\n    _check_task_file_invariants(tasks, violations)\n    return violations", "replacement_text": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    workplan = _resolve_workplan(plan)\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n        return violations\n    tasks = _collect_tasks(workplan)\n    _check_task_file_invariants(tasks, violations)\n    return violations\n\n\ndef _resolve_workplan(plan) -> object:\n    \"\"\"Extract the workplan from plan, falling back to strategy.parallelisable_workplan.\"\"\"\n    workplan = getattr(plan, 'workplan', None)\n    if not workplan:\n        strategy = getattr(plan, 'strategy', None)\n        if strategy:\n            workplan = getattr(strategy, 'parallelisable_workplan', None)\n    return workplan\n\n\ndef _collect_tasks(workplan) -> list:\n    \"\"\"Collect all tasks from workplan groups.\"\"\"\n    groups = getattr(workplan, 'groups', []) or []\n    tasks: list = []\n    for group in groups:\n        tasks.extend(getattr(group, 'tasks', []) or [])\n    return tasks\n\n\ndef _check_task_file_invariants(tasks: list, violations: list[str]) -> None:\n    \"\"\"Check that each task has exactly 1 file and no file is shared across tasks.\"\"\"\n    seen: set[str] = set()\n    for task in tasks:\n        fps = getattr(task, 'file_paths', None) or []\n        if len(fps) != 1:\n            violations.append(f\"task {getattr(task, 'id', '?')} lists {len(fps)} files (exactly 1 required)\")\n        for fp in fps:\n            if fp in seen:\n                violations.append(f'file collision: {fp} in multiple tasks')\n            seen.add(fp)"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 1, "end_line": 100})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_resolve_workplan"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_collect_tasks"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_check_task_file_invariants"})

[tool-call] remember({"note": "_resolve_workplan CC=3, _collect_tasks CC=4, _check_task_file_invariants CC=6 (needs reduction to ≤5). The CC for _check_task_file_invariants comes from the nested loop (for fp in fps inside for task in tasks) plus the if conditions. I need to extract the inner loop body into another helper to reduce CC."})

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_check_task_file_invariants", "new_function_code": "def _check_task_file_invariants(tasks: list, violations: list[str]) -> None:\n    \"\"\"Check that each task has exactly 1 file and no file is shared across tasks.\"\"\"\n    seen: set[str] = set()\n    for task in tasks:\n        fps = getattr(task, 'file_paths', None) or []\n        _check_single_file_task(task, fps, violations)\n        _check_file_collisions(fps, seen, violations)\n\n\ndef _check_single_file_task(task, fps: list, violations: list[str]) -> None:\n    \"\"\"Check that a task lists exactly 1 file.\"\"\"\n    if len(fps) != 1:\n        violations.append(f\"task {getattr(task, 'id', '?')} lists {len(fps)} files (exactly 1 required)\")\n\n\ndef _check_file_collisions(fps: list, seen: set[str], violations: list[str]) -> None:\n    \"\"\"Check for file path collisions across tasks.\"\"\"\n    for fp in fps:\n        if fp in seen:\n            violations.append(f'file collision: {fp} in multiple tasks')\n        seen.add(fp)"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_check_single_file_task"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_check_file_collisions"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants"})

[tool-call] final_result({"task_id": "reduce-cc-check_plan_invariants", "status": "done", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored check_plan_invariants from CC=13 to CC=2 by extracting _resolve_workplan(), _collect_tasks(), _check_task_file_invariants(), _check_single_file_task(), and _check_file_collisions(). All helpers are private and have CC≤4.", "notes": "All helper functions pass AST verification with CC≤5. The main function check_plan_invariants has CC=2. No public signatures were modified.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true, "verdict_errors": "", "verdict_diff": "", "dep_pointers": []})