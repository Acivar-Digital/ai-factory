[text] The issue is that local imports inside functions are being flagged as "unauthorized_import". I need to move `from datetime import datetime` and `from sqlalchemy import func` to the top level of the file, then remove the local imports from inside the functions

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[text] I need to move both `datetime` and `func` imports to the top level and remove the local imports from inside the functions.



[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "from datetime import UTC\nfrom uuid import UUID\n\nfrom sqlalchemy import select, update", "replacement_text": "from datetime import UTC, datetime\nfrom uuid import UUID\n\nfrom sqlalchemy import func, select, update"})

[text] Now remove the local imports from inside the functions.




[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "def _is_promo_expired(promo: PromoCode) -> bool:\n    from datetime import datetime\n\n    return promo.expires_at is not None and promo.expires_at < datetime.now(UTC)", "replacement_text": "def _is_promo_expired(promo: PromoCode) -> bool:\n    return promo.expires_at is not None and promo.expires_at < datetime.now(UTC)"})

[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "async def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    from sqlalchemy import func\n\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))", "replacement_text": "async def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))"})

[text] Now let me verify the edit passes all checks.




[tool-call] verify_edit({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "validate_promo_code"})

[text] The verify_edit passes now. Let me also verify the helper functions and check the overall file state.




[tool-call] verify_edit({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "_is_promo_expired"})

[tool-call] verify_edit({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "_is_promo_at_max_uses"})

[text] All three functions pass verification with CC ≤ 5 and no unauthorized imports. Let me submit the final result.



[tool-call] final_result({"task_id": "reduce-cc-5-functions-3-files", "status": "done", "files_changed": ["factory/temp/src2/core/services/billing.py"], "diff_summary": "Refactored validate_promo_code in factory/temp/src2/core/services/billing.py from CC=6 to CC=4 by extracting two helper functions (_is_promo_expired CC=2, _is_promo_at_max_uses CC=2). Moved datetime and func imports to top level to eliminate unauthorized_import flags.", "notes": "The previous attempt modified the wrong billing.py file (factory/temp/src2/billing.py instead of factory/temp/src2/core/services/billing.py). I corrected this by refactoring validate_promo_code in the correct file. The refactoring uses helper extraction: _is_promo_expired and _is_promo_at_max_uses reduce the main function's CC from 6 to 4. All imports moved to top level to pass verify_edit's unauthorized_import check. The other two files (agents.py, forecast_store.py) were already refactored by the previous attempt.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true, "verdict_diff": "validate_promo_code CC reduced from 6 to 4 via helper extraction (_is_promo_expired CC=2, _is_promo_at_max_uses CC=2). All imports moved to top level.", "verdict_errors": ""})