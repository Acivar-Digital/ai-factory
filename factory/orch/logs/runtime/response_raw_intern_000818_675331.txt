[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] remember({"note": "Target: Refactor `_blocker_findings_from_risks` in factory/temp/factory/infra/validation.py to reduce CC from 9 to <=5. Strategy: extract private helpers `_is_blocker_risk`, `_risk_requires_unresolved`, `_promote_risk_to_finding`, `_unresolved_label` to reduce branching in the main function. Use guard clauses. Preserve signature. Only modify this one function area."})

[tool-call] grep({"relative_path": "factory/temp/factory/infra/validation.py", "pattern": "def _blocker_findings_from_risks", "output_mode": "content", "-n": "true"})

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_blocker_findings_from_risks", "new_function_code": "def _blocker_findings_from_risks(findings: list[ReviewFinding], risks: list[AuditRisk], known_task_ids: set[str]) -> tuple[list[ReviewFinding], list[str]]:\n    \"\"\"Anti-laziness guard for the self-graded red-team verdict.\n\n    The red-team model emits BOTH `findings` (which route re-execution) and\n    `risks` (which name offending tasks). A lazy model can emit Critical/High\n    `risks` flagging real defects yet leave `findings` empty — which lets\n    `red_team_passed` return True and skip re-execution entirely, so the\n    defects ship to ops unreviewed.\n\n    Any Critical/High `AuditRisk` that carries a `task_id` inside the approved\n    plan but has NO matching blocker `ReviewFinding` is promoted to a blocker\n    finding, so the offending task is actually re-coded. Risks that name a\n    defect but carry no resolvable `task_id` are returned separately so the\n    caller can HARD FAIL them as unresolvable (mirrors the rubric_cube\n    global-blocker rule).\"\"\"\n    blocker_task_ids = _collect_blocker_task_ids(findings)\n    augmented = list(findings)\n    unresolved_global: list[str] = []\n    for r in risks:\n        if not _is_significant_risk(r):\n            continue\n        if not _has_resolvable_task(r, known_task_ids):\n            unresolved_global.append(_unresolved_label(r))\n            continue\n        if r.task_id in blocker_task_ids:\n            continue\n        augmented.append(_promote_risk_to_finding(r))\n        blocker_task_ids.add(r.task_id)\n    return (augmented, unresolved_global)\n\n\ndef _collect_blocker_task_ids(findings: list[ReviewFinding]) -> set[str]:\n    return {f.task_id for f in findings if f.severity == 'blocker'}\n\n\ndef _is_significant_risk(r: AuditRisk) -> bool:\n    return r.severity in ('Critical', 'High')\n\n\ndef _has_resolvable_task(r: AuditRisk, known_task_ids: set[str]) -> bool:\n    return bool(r.task_id) and r.task_id in known_task_ids\n\n\ndef _unresolved_label(r: AuditRisk) -> str:\n    return r.component or r.task_id or '<anonymous>'\n\n\ndef _promote_risk_to_finding(r: AuditRisk) -> ReviewFinding:\n    return ReviewFinding(\n        task_id=r.task_id,\n        severity='blocker',\n        file=r.component,\n        line=None,\n        message=f'[auto-derived from {r.severity} risk] {r.description}',\n        suggestion=r.mitigation,\n    )"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_blocker_findings_from_risks"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_collect_blocker_task_ids"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_is_significant_risk"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_has_resolvable_task"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_unresolved_label"})

[tool-call] verify_edit({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "_promote_risk_to_finding"})

[tool-call] final_result({"status": "done", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored _blocker_findings_from_risks (CC 9→5) by extracting 5 private helpers: _collect_blocker_task_ids, _is_significant_risk, _has_resolvable_task, _unresolved_label, _promote_risk_to_finding. All helpers have CC ≤ 3. Public signature unchanged.", "notes": "All target functions now have CC ≤ 5. The main function went from CC=9 to CC=5, and all extracted helpers have CC ≤ 3. No public signatures were modified."})

[tool-call] final_result({"status": "done", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored _blocker_findings_from_risks (CC 9→5) by extracting 5 private helpers: _collect_blocker_task_ids, _is_significant_risk, _has_resolvable_task, _unresolved_label, _promote_risk_to_finding. All helpers have CC ≤ 3. Public signature unchanged.", "notes": "All target functions now have CC ≤ 5. The main function went from CC=9 to CC=5, and all extracted helpers have CC ≤ 3. No public signatures were modified.", "task_id": "reduce-cc-validation"})