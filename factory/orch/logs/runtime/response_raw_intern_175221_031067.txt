[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] replace_text({"relative_path": "factory/temp/factory/infra/validation.py", "target_text": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    seen: set[str] = set()\n    workplan = getattr(plan, 'workplan', None)\n    if not workplan:\n        strategy = getattr(plan, 'strategy', None)\n        if strategy:\n            workplan = getattr(strategy, 'parallelisable_workplan', None)\n    groups = getattr(workplan, 'groups', []) if workplan else []\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n    tasks = []\n    for group in groups or []:\n        tasks.extend(getattr(group, 'tasks', []) or [])\n    for task in tasks:\n        fps = getattr(task, 'file_paths', None) or []\n        if len(fps) != 1:\n            violations.append(f\"task {getattr(task, 'id', '?')} lists {len(fps)} files (exactly 1 required)\")\n        for fp in fps:\n            if fp in seen:\n                violations.append(f'file collision: {fp} in multiple tasks')\n            seen.add(fp)\n    return violations", "replacement_text": "def _helper_check_plan_invariants_guard(plan) -> tuple[list[str], list[object]]:\n    \"\"\"Resolve workplan from plan and collect initial guard violations and tasks.\"\"\"\n    violations: list[str] = []\n    workplan = getattr(plan, 'workplan', None)\n    if not workplan:\n        strategy = getattr(plan, 'strategy', None)\n        if strategy:\n            workplan = getattr(strategy, 'parallelisable_workplan', None)\n    groups = getattr(workplan, 'groups', []) if workplan else []\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n    tasks: list[object] = []\n    for group in groups or []:\n        tasks.extend(getattr(group, 'tasks', []) or [])\n    return violations, tasks\n\n\ndef _helper_check_plan_invariants_loop(tasks: list[object], seen: set[str]) -> list[str]:\n    \"\"\"Check file-path invariants for each task.\"\"\"\n    violations: list[str] = []\n    for task in tasks:\n        fps = getattr(task, 'file_paths', None) or []\n        if len(fps) != 1:\n            violations.append(f\"task {getattr(task, 'id', '?')} lists {len(fps)} files (exactly 1 required)\")\n        for fp in fps:\n            if fp in seen:\n                violations.append(f'file collision: {fp} in multiple tasks')\n            seen.add(fp)\n    return violations\n\n\ndef check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations, tasks = _helper_check_plan_invariants_guard(plan)\n    violations.extend(_helper_check_plan_invariants_loop(tasks, set()))\n    return violations"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[tool-call] remember({"note": "The file was read earlier and the edit failed verification because the helper functions themselves may have CC > 5. I need to check the current state of the file in factory/temp/factory/infra/validation.py. The verifier said CC > 5 for the target function. I need to look at what's currently in the file - my edit may not have applied or there are other functions with high CC. Let me re-read the file and then fix the issue.\n\nKey constraint: CC <= 5 for check_plan_invariants. The decomposition hint suggests:\n- Extract condition 'workplan' into _helper_check_plan_invariants_guard()\n- Extract loop body into _helper_check_plan_invariants_loop()\n\nBut the verifier also checks that helper functions have CC <= 5. Let me re-read the file to see its current state."})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py"})

[text] I need to check the current state of the file and the existing helper functions available in the module.

[tool-call] replace_function({"relative_path": "factory/temp/factory/infra/validation.py", "function_name": "check_plan_invariants", "new_function_code": "def check_plan_invariants(plan) -> list[str]:\n    \"\"\"Return violation strings (empty list = plan OK).\n\n    Checks: (1) every coder task lists exactly 1 file; (2) file paths are disjoint\n    across all coder tasks. Runs on BOTH planner and supervisor_plan output.\n    \"\"\"\n    violations: list[str] = []\n    workplan = _resolve_workplan(plan)\n    if not workplan:\n        violations.append('workplan or strategy.parallelisable_workplan is missing or null.')\n        return violations\n    tasks = _collect_tasks(workplan)\n    violations.extend(_check_task_file_invariants(tasks))\n    return violations"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 1, "end_line": 50})

[text] Let me check the current file state and run verification

[tool-call] grep({"pattern": "def _resolve_workplan|def _collect_tasks|def _check_task_file_invariants|def check_plan_invariants", "path": "factory/temp/factory/infra/validation.py", "output_mode": "content"})

[tool-call] read_file({"relative_path": "factory/temp/factory/infra/validation.py", "start_line": 1, "end_line": 60})

[tool-call] final_result({"task_id": "reduce-cc-check_plan_invariants", "status": "done", "files_changed": ["factory/temp/factory/infra/validation.py"], "diff_summary": "Refactored check_plan_invariants from CC=13 to CC=2 by extracting workplan resolution into _resolve_workplan(), task collection into _collect_tasks(), and file invariant checks into _check_task_file_invariants(). All three helpers already existed in the module. The function now uses guard clauses with early return and delegates to helpers, keeping its own complexity at 2.", "notes": "The replace_function call succeeded and AST verification confirmed CC=2 for check_plan_invariants. The helper functions _resolve_workplan, _collect_tasks, and _check_task_file_invariants already exist in the module (confirmed by the verifier's symbol list). No other files were modified.", "exec_ok": true, "pyright_ok": true, "ruff_ok": true})