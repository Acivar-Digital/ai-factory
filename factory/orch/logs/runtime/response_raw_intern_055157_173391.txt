[tool-call] remember({"note": "Task: Refactor validate_promo_code in factory/temp/src2/core/services/billing.py to reduce CC from 6 to ≤5. Use guard clauses, early returns, or helper extraction. Only modify billing.py. Must pass ruff check and find_cc_nested.py verification."})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[text] Now I'll refactor `validate_promo_code` to reduce CC from 6 to ≤5 by extracting the two compound conditions into private helper functions.


[tool-call] replace_function({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "validate_promo_code", "new_function_code": "def _is_promo_expired(promo: PromoCode) -> bool:\n    from datetime import datetime\n\n    return bool(promo.expires_at and promo.expires_at < datetime.now(UTC))\n\n\ndef _is_promo_at_max_uses(promo: PromoCode) -> bool:\n    return bool(promo.max_uses and promo.current_uses >= promo.max_uses)\n\n\nasync def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    from sqlalchemy import func\n\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))\n    promo = result.scalar_one_or_none()\n    if not promo:\n        return None\n    if _is_promo_expired(promo):\n        return None\n    if _is_promo_at_max_uses(promo):\n        return None\n    return promo"})

[tool-call] remember({"note": "First attempt failed because: (1) imports inside function body are unauthorized — must use top-level imports or avoid adding new imports; (2) CC still 6 because the main function still has 4 branches (if not promo, if expired, if max_uses, return). Need to extract more aggressively. The datetime and sqlalchemy imports already exist at module level, so I should not add imports inside the function. Also need to reduce CC in validate_promo_code itself to ≤5."})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[tool-call] replace_function({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "validate_promo_code", "new_function_code": "async def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    from sqlalchemy import func\n\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))\n    promo = result.scalar_one_or_none()\n    if not promo or _is_promo_expired(promo) or _is_promo_at_max_uses(promo):\n        return None\n    return promo"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[tool-call] replace_function({"relative_path": "factory/temp/src2/core/services/billing.py", "function_name": "validate_promo_code", "new_function_code": "def _is_promo_expired(promo: PromoCode) -> bool:\n    from datetime import datetime\n\n    return bool(promo.expires_at and promo.expires_at < datetime.now(UTC))\n\n\ndef _is_promo_at_max_uses(promo: PromoCode) -> bool:\n    return bool(promo.max_uses and promo.current_uses >= promo.max_uses)\n\n\nasync def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    from sqlalchemy import func\n\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))\n    promo = result.scalar_one_or_none()\n    if not promo or _is_promo_expired(promo) or _is_promo_at_max_uses(promo):\n        return None\n    return promo"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py", "start_line": 1, "end_line": 50})

[tool-call] remember({"note": "Now I can see the current state of billing.py. The function validate_promo_code has inline imports (from datetime import datetime and from sqlalchemy import func). The CC checker says CC=6 because it counts: 1 base + 1 (not promo) + 1 (expires_at check) + 1 (max_uses check) = 4 branches... wait, let me recount. The CC checker counts: function entry (1) + not promo (2) + expires_at and (3) + max_uses and (4) = 4? But it says CC=6. Maybe it counts each condition in boolean expressions separately: promo.expires_at (1), promo.expires_at < datetime.now (2), promo.max_uses (3), promo.current_uses >= promo.max_uses (4). Plus the two if not promo = 6 total. So I need to reduce it to ≤5. Strategy: extract _is_promo_expired and _is_promo_at_max_uses as module-level helpers (no inline imports needed since UTC is already imported at top level). Then in validate_promo_code, use a single combined guard: if not promo or _is_promo_expired(promo) or _is_promo_at_max_uses(promo): return None. This gives CC = 1 (base) + 1 (not promo) + 1 (expired) + 1 (max_uses) = 4. But the helper functions also need CC ≤ 5 each. _is_promo_expired has: bool() + and = 2 CC. _is_promo_at_max_uses has: bool() + and = 2 CC. All ≤5. Key: I must NOT add inline imports inside the function body. The datetime and func imports should be removed from inside validate_promo_code since func is already imported at module level (line 4: from sqlalchemy import select, update). Wait, func is NOT imported at module level - only select and update are. So I need to add func to the top-level import. And datetime is not imported at module level either - only UTC is. So I need to add datetime to the top-level import too. Or I can restructure _is_promo_expired to not need datetime by comparing differently. Actually, looking at the top of the file: `from datetime import UTC` - I can change this to `from datetime import UTC, datetime`. And change `from sqlalchemy import select, update` to `from sqlalchemy import select, update, func`. This avoids inline imports."})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py", "start_line": 1, "end_line": 15})

[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "from datetime import UTC", "replacement_text": "from datetime import UTC, datetime"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py", "start_line": 1, "end_line": 15})

[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "from sqlalchemy import select, update", "replacement_text": "from sqlalchemy import select, update, func"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py", "start_line": 1, "end_line": 15})

[tool-call] replace_text({"relative_path": "factory/temp/src2/core/services/billing.py", "target_text": "async def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    from datetime import datetime\n    from sqlalchemy import func\n\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))\n    promo = result.scalar_one_or_none()\n    if not promo:\n        return None\n    if promo.expires_at and promo.expires_at < datetime.now(UTC):\n        return None\n    if promo.max_uses and promo.current_uses >= promo.max_uses:\n        return None\n    return promo", "replacement_text": "def _is_promo_expired(promo: PromoCode) -> bool:\n    return bool(promo.expires_at and promo.expires_at < datetime.now(UTC))\n\n\ndef _is_promo_at_max_uses(promo: PromoCode) -> bool:\n    return bool(promo.max_uses and promo.current_uses >= promo.max_uses)\n\n\nasync def validate_promo_code(session: AsyncSession, code: str) -> PromoCode | None:\n    result = await session.execute(select(PromoCode).where(func.lower(PromoCode.code) == func.lower(code)))\n    promo = result.scalar_one_or_none()\n    if not promo or _is_promo_expired(promo) or _is_promo_at_max_uses(promo):\n        return None\n    return promo"})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py", "start_line": 1, "end_line": 15})

[tool-call] read_file({"relative_path": "factory/temp/src2/core/services/billing.py"})

[tool-call] final_result({"status": "blocked", "files_changed": ["factory/temp/src2/core/services/billing.py"], "diff_summary": "Refactor validate_promo_code to reduce CC from 6 to ≤5", "notes": "The verification tool keeps rejecting my edits because it reports CC > 5 for validate_promo_code. I need to extract more aggressively. The issue is that the combined guard `if not promo or _is_promo_expired(promo) or _is_promo_at_max_uses(promo)` still has CC=4 for the function (base + 3 or-conditions). But the tool says CC=6, which suggests it's counting the or-conditions as separate CC points AND the function entry. Let me try a different approach: use early return guard clauses instead of a combined condition, and ensure no inline imports remain. I also need to move datetime and func imports to the top of the file. Let me re-read the file first to see its current state, then make a clean edit.", "task_id": "a101k"})